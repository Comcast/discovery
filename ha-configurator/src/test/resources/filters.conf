# Service registration path filters.  One per line.  These are service filters
# used by the discovery client.
#
# This configuration will pull in all services from all regions if those services
# are registered with the Zookeeper cluster.
#
**
